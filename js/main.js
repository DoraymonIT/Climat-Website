
  AOS.init();

  
  var options = {
    strings: ['FROID .', 'CHAUD .'],
    typeSpeed: 120,
    loop: true,
    loopCount: Infinity,
    cursorChar: '.',
  };
  
  var typed = new Typed('#fc', options);